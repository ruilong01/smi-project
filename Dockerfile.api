FROM node:24-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY server ./server
COPY src/data/generated ./src/data/generated

ENV NODE_ENV=production
ENV API_HOST=0.0.0.0
ENV API_PORT=8787

EXPOSE 8787

CMD ["node", "server/server.mjs"]
